import React from 'react';
import {View} from 'react-native';
import {CharacterCardProps} from '../../types';

export const CharacterCard = ({data, onPress}: CharacterCardProps) => {
  return <View>CharacterCard</View>;
};
